package Interfaces;

/**
 * The ISearchable interface provides a contract for searchable objects.
 * It defines a method to search for items based on a specified attribute and query.
 * 
 * @param <T> the type of objects that this interface can search
 * @param <E> the type of enum that represents the attributes to search by
 * 
 * The enum Attribute must be overridden when being used with appropriate classes.
 */
public interface ISearchable{
}
